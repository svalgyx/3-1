for using System;

namespace HW3_1
{
    class Program
    {
        static void Main(string[] args)
        {
            // r в коде это rows
            // input_c - (colums) - ввести столбцы для двумерного массива, c - столбцы

            // одномерные массивы
            // Console.WriteLine("Input len of the array: ");
            // int input_length = int.Parse(Console.ReadLine());
            // Console.WriteLine("Yes for inputing an array and no for filling it with random nums ");
            // string input = Console.ReadLine();
            // bool init_input = false;
            // if (input == "yes")
            // {
            //    init_input = true;
            // }
            // OneStep one_step = new OneStep(input_length, init_input);
            // one_step.NewArray();
            // one_step.WriteArray();
            // one_step.ArrayMod100();
            // one_step.WriteArray();
            // one_step.ClearArray();
            // one_step.WriteArray();
            // Console.WriteLine($"average : {one_step.Average()}");

            // двумерные массивы
            // Console.WriteLine("Input rows of two-dimensional array: ");
            // int input_r = int.Parse(Console.ReadLine());
            // Console.WriteLine("Input colums of two-dimension array: ");
            // int input_c = int.Parse(Console.ReadLine());
            // Console.WriteLine("yes for inputing array, no for filling with rand numbers ");
            // string input = Console.ReadLine();
            // bool init_input = false;
            // if (input == "yes")
            // {
            //    init_input = true;
            // }
            // TwoStep two_step = new TwoStep(input_r, input_c, init_input);
            // two_step.CreateArray();
            // two_step.WriteArray();
            // Console.WriteLine($"average: {two_step.Average()}");

            // ступенчатые массивы
            // Console.WriteLine("Input rows (rows of jagged array: ");
            // int input_r = int.Parse(Console.ReadLine());
            // Console.WriteLine("yes for inputing an array and no for filling it with random nums ");
            // string input = Console.ReadLine();
            // bool init_input = false;
            // if (input == "yes")
            // {
            //    init_input = true;
            // }
            // JaggedArray jagged_array = new JaggedArray(input_r, init_input);
            // jagged_array.CreateArray();
            // jagged_array.WriteArray();
            // Console.WriteLine($"average: {jagged_array.Average()}");
            // double[] average_values = jagged_array.AverageValues();
            // string output = "";
            // for (int i = 0; i < input_r; i++)
            // {
            //    output += average_values[i].ToString() + " ";
            // }
            // Console.WriteLine($"average values: {output}");
            // jagged_array.ChangeEvenNums();
            // jagged_array.WriteArray();
        }
    }
}
